rules_version = '2';

// Storage locked down until Phase 4 product image uploads.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
